// on slider
$(document).ready(function(){
  $('.slider-comment').slick({
    dots: true
  });
});

$('.fade').slick({
  dots: true,
  infinite: true,
  speed: 500,
  fade: true,
  cssEase: 'linear'
});
